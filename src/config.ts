export const config = {
    // Default to localhost for dev, but this should be configured by the user
    n8nBaseUrl: import.meta.env.VITE_N8N_BASE_URL || 'https://n8n.srv932791.hstgr.cloud/',
};
